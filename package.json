{
  "private": true,
  "name": "yarnv2-workspaces-ts-node-bug",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "start-foo": "ts-node -P ./tsconfig.json ./packages/foo/src/index.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "ts-node": "latest",
    "ts-node-dev": "latest",
    "typescript": "beta"
  }
}
